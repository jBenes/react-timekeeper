import { MODE } from '../../helpers/constants';
export declare const wrapper: (hour24Mode: boolean, mode: MODE) => any;
export declare const options: any;
export declare const option: (active: boolean) => any;
